{
  "name": "json-server-mock",
  "version": "1.0.0",
  "description": "json-server example configuration",
  "main": "index.js",
  "private": true,
  "watch": {
    "json-server": "./server/**/*.{js,json}"
  },
  "scripts": {
    "json-server": "node server/index.js",
    "mock": "npm-watch json-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.12.1",
    "yargs": "^11.0.0"
  },
  "devDependencies": {
    "npm-watch": "^0.6.0"
  }
}
